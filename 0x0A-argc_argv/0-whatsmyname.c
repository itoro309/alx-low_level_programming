#include "main.h"
#include <stdio.h>

/**
 * main - Prints
 * @argc: Number of command
 * @argv: Array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
